@model PagedList.IPagedList<WebView.WebService._base.CityMaintenance>
@{
    ViewBag.Title = "List";
}

<script type="text/javascript">

    function GetDetailsCallBack(data) {
        document.getElementById("divDetails").style.display = "block";
        SetElement("txtCity", data.CityName);
        SetElement("txtStateID", data.StateID);
        SetElement("txtSName", data.StateName);

        SetElement("CityIdVal", data.CityId);
        SetElement("targetCity", data.CityName);
        document.getElementById("targetCity").innerHTML = data.CityName;
        document.getElementById("btnClose").disabled = false;
        //        alert( document.getElementById("txtStateID").value )
       
    }

    function SaveUpdateCallBack(data) {
        document.getElementById("txtCity").readOnly = true;
        document.getElementById("txtStateID").readOnly = true;
        document.getElementById("txtSName").readOnly = true;
        document.getElementById("btnSave").disabled = true;
        
        if (data.Success == true) {
            alert("Successfully Updated.");
            document.getElementById("btnSave").disabled = false;
            document.getElementById("divDetails").style.display = "none";
            window.location.reload();
        }
    }

    jQuery(document).ready(function ($) {

        $("#rounded-corner tbody tr").bind("click", function () {
            var t = $(this).find("#gid").text();
            var sCode = $(this).find("#sCode").text();
            $("#divDetails").dialog("open");

            $.ajax({
                url: '@Url.Action("GetDetails", "CityMaintenance")',
                type: "POST",
                dataType: "html",
                data: {
                    "id": t,
                    "SCode": sCode,
                    "random": Math.random()
                },
                beforeSend: function () { },
                success: function (data) {
                    var res = jQuery.parseJSON(data);
                    var CityIDList;

                    GetDetailsCallBack(res);
                    //alert(res.StateList);
                    CityIDList = res.StateListCity.split("|");

                    var dataListName = "<option selected value='" + res.StateID + "'>" + res.StateID + "</option>";

                    for (i = 0; i < CityIDList.length; i++) {
                        if (CityIDList[i] != "") {

                            dataListName += "<option value='" + CityIDList[i] + "'>" + CityIDList[i] + "</option>";

                        }
                    }
                    $("#txtStateID").html(dataListName);

                    var StateCode = "";
                    StateCode = res.StateName;
                    var StateCode = "<option selected value='" + StateCode + "'>" + StateCode + "</option>";
                   // StateCode = "<option value='" + StateCode + "'>" + StateCode + "</option>";
                    $("#txtSName").html(StateCode);


                    //                    CityIDList = res.StateName.split("|");

                    //                    var dataListName = "<option selected value='" + res.StateID + "'>" + res.StateID + "</option>";

                    //                    for (i = 0; i < CityIDList.length; i++) {
                    //                        if (CityIDList[i] != "") {

                    //                            dataListName += "<option value='" + CityIDList[i] + "'>" + CityIDList[i] + "</option>";

                    //                        }
                    //                    }
                    //                    $("#txtSName").html(dataListName);
                }
            });
        });

        var close = function () {
            $(this).dialog("close");
        }

        $("#divDetails").dialog({
            autoOpen: false,
            minHeight: 'auto',
            width: 700,
            modal: true,

            buttons: {
            },
            close: function () {
                //allFields.val("").removeClass("ui-state-error");
            }
        });

        $(function () {
            $('#btnSearch').click(function () {
                window.location = "List?searchKey=" + document.getElementById("searchKey").value + "&searchCity=" + document.getElementById("searchCity").value; //Where List is the function in the controller.

            });

            $('#searchKey').change(function () {
                window.location = "List?searchKey=" + document.getElementById("searchKey").value + "&searchCity=" + document.getElementById("searchCity").value; //Where List is the function in the controller.
            });

            $('#btnClose').click(function () {
                $("#divDetails").dialog("close");
            });

            $('#btnNewJob').click(function () {
                window.location = "NewCityMaintenance";
            });

            $('#txtStateID').click(function () {
                $.ajax({
                    url: "/CityMaintenance/DescriptionName",
                    type: "POST",
                    dataType: "html",
                    data: {
                        "StateCode": document.getElementById("txtStateID").value,
                        "random": Math.random()
                    },
                    beforeSend: function () { },
                    success: function (data) {
                        var res = jQuery.parseJSON(data);

                        StateCode = res.Des.split(":");

                        var StateListName = "<option selected value='" + StateCode[0] + "'>" + StateCode[0] + "</option>";

                        for (i = 0; i < StateCode.length; i++) {
                            if (StateCode[i] != "") {

                                StateListName += "<option value='" + StateCode[i] + "'>" + StateCode[i] + "</option>";
                            }
                        }
                        $("#txtSName").html(StateListName);
                    }
                });
            });

            $("#btnNewJob").button();
            $("#btnSearch").button();
            $("#btnClose").button();
            $("#btnDelete").button();
            $("#btnSave").button();
         
            $('#btnDelete').click(function () {
                var cmsg = confirm('Are you sure you want to Delete?');
                if (cmsg != 0) {
                    $.ajax({
                        url: "/CityMaintenance/DeleteData",
                        type: "POST",
                        dataType: "html",
                        data: {
                            "targetCity": document.getElementById("targetCity").value,
                            "random": Math.random()
                        },
                        beforeSend: function () { },
                        success: function (data) {
                            var res = jQuery.parseJSON(data);

                            if (res.Success == true) {
                                alert("Deleted");
                                window.location.reload();
                            }
                            else {
                                alert("Delete failed.");
                            }
                        }
                    });
                }
            });
        });
    });

    function ShowIt() {
        document.getElementById("divLoading").style.visibility = "visible";
    }

    function HideIt() {
        document.getElementById("divLoading").style.visibility = "hidden";
    }

</script>

<br />
<h2>City Maintenance List</h2>
<div style="height:600px">
    <div>
        <table style="width:850px;">
            <tr>
                <td style="width:200px;"><input type="text" id="searchKey" name="searchKey"  value="@ViewBag.searchKey"/></td>
                 <td>State Name : @Html.DropDownList("searchCity", (IEnumerable<SelectListItem>)ViewBag.LCityID, new { style = "width:150px" })</td> 
                 <td><input type="button" value="Search" id="btnSearch" /></td>
                <td style="text-align:right"><input type="button" value="Add New" id="btnNewJob" /></td>
            </tr>
        </table>
    </div><br />
@if (Model.Count() > 0)
{
    <table id="rounded-corner" summary="City Maintenance List" width="850px">
        <thead>
            <tr>
                <th scope="col">City Name</th>
                <th scope="col">State ID</th>   
                <th scope="col">State Name</th>
            </tr>
        </thead>
        <tbody>
        @foreach (var res in Model)
        {
            <tr class="odd">
                
                <td id="gid">@res.CITY_NAME</td>
                <td id="sCode">@res.STATE_CODE </td>
                <td>@res.DESCRIPTION_STATE</td>  
                
                                     
                @*<td>@Ajax.ActionLink("Details", "GetDetails", new { id = res.PTT_ID }, new AjaxOptions
               {
                   HttpMethod = "POST",
                   OnBegin = "ShowIt",
                   OnComplete = "HideIt",
                   UpdateTargetId = "divResultText",
                   OnSuccess = "GetDetailsCallBack"
               })</td>*@
            </tr>    
        }
        </tbody>
    </table>
    <div>
        Page @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber)
        of @Model.PageCount
        &nbsp;
        @if (Model.HasPreviousPage)
        {
            @Html.ActionLink("<<", "List", new { searchKey = ViewBag.searchKey, searchCity = ViewBag.CitySearch2, page = 1 })
            @Html.Raw("&nbsp;");
            @Html.ActionLink("< Prev", "List", new { searchKey = ViewBag.searchKey, searchCity = ViewBag.CitySearch2, page = Model.PageNumber - 1 })
        }
        else
        {

        }
        &nbsp;
        @if (Model.HasNextPage)
        {
            @Html.ActionLink("Next >", "List", new { searchKey = ViewBag.searchKey, searchCity = ViewBag.CitySearch2, page = Model.PageNumber + 1 })
            @Html.Raw("&nbsp;");
            @Html.ActionLink(">>", "List", new { searchKey = ViewBag.searchKey, searchCity = ViewBag.CitySearch2, page = Model.PageCount })
        }
        else
        {

        }
        <br/><b> Total @Model.TotalItemCount  records</b>
    </div>
}
else
{
        @:<br /><span style="font-size:12px;">No record found.</span> 
    }
    <br />
    <div id="divLoading" style="visibility:hidden"><img src="@Url.Content("~/Content/images/rel_interstitial_loading.gif")" alt="loading..." /></div>
    <div id="divResultText"></div>
    <br />
    <div id="divDetails" title="Details" style="padding-left:40px; padding-top:30px;">      
        <input type="hidden" id="targetCity" value="" />
        @using (Ajax.BeginForm("UpdateData", new AjaxOptions()
        {
            OnBegin = "ShowIt",
            OnComplete = "HideIt",
            OnSuccess = "SaveUpdateCallBack"
        }))
        {
        <div id="list-details" style="height:120px">
           <input type="hidden" id="CityIdVal" name="CityIdVal" />
            <table width="400px">
            <tr>
                    <td style="width:150px; background-color:#e8eef4">City Name</td>
                    <td><input type="text" id="txtCity" name="txtCity" /></td>
                    <td style=" border-top-color:White; border-bottom-color:White"></td>                                      
                </tr>
                <tr>
                    <td style="width:150px; background-color:#e8eef4">State Code</td>
                    <td><select id="txtStateID" name="txtStateID">
                    </select></td>
                    <td style=" border-top-color:White; border-bottom-color:White"></td>                                      
                </tr>
                
                 <tr>
                    <td style="width:150px; background-color:#e8eef4">State Name</td>
                    <td><select id="txtSName" name="txtSName">
                    </select></td>
                    <td style=" border-top-color:White; border-bottom-color:White"></td>                                      
                </tr>   
                @***********************************************************************@        
            </table>
            <br />            
            <div style="height:30px; margin-top:10px; margin-left:10px;">
                <input type="button" value="Delete" id="btnDelete" style="height:30px;width:80px; vertical-align:top;" />
                &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
                &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
                &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
                &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
                &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
                <input type="submit" value="Save" id="btnSave" style="height:30px;width:80px; vertical-align:top;" />
                <input type="button" value="Cancel" id="btnClose" style="height:30px;width:80px;" />
            </div>
        </div>
        }
        <br />
        <br />
    </div>
</div>
    


